# Database (Postgres — see docker-compose.yml for local dev)
DATABASE_URL="postgresql://user:password@localhost:5430/vendordb"

# Server
PORT=3000

# Auth
JWT_ACCESS_SECRET="replace-with-a-long-random-string"
JWT_ACCESS_EXPIRES_IN="15m"
